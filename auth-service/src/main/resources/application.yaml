spring:
  application:
    name: auth-service

  config:
    import: optional:configserver:http://localhost:8888

jwt:
  secret: ${JWT_SECRET:my-super-secret-key-for-airline-auth-service-123456}
  expiration: 3600000